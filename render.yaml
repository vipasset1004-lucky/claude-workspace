services:
  - type: web
    name: divergence-screener
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn web_server:app --bind 0.0.0.0:$PORT --timeout 300 --workers 2 --threads 4
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
